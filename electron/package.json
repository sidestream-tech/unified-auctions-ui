{
  "name": "unified-auctions",
  "version": "1.0.0",
  "description": "MakerDAO Unified Auctions Electron App",
  "main": "main.js",
  "build": {
    "appId": "com.makerdao.unified-auctions",
    "mac": {
      "category": "public.app-category.finance"
    },
    "directories": {
      "output": "out"
    },
    "files": [
      "main.js",
      "dist/**/*"
    ]
  },
  "scripts": {
    "preinstall": "cd ../frontend && npm ci",
    "prebuild": "(cd ../frontend && npm run generate) && npx copyfiles -a -u 2 \"../frontend/dist/**/*\" .",
    "start": "electron .",
    "build": "rm -rf out && electron-builder"
  },
  "author": "SIDESTREAM GmbH",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^22.1.0",
    "electron-builder": "^23.6.0"
  }
}
